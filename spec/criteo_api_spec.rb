require 'spec_helper'

describe CriteoApi do
  it 'has a version number' do
    expect(CriteoApi::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
